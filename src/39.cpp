#include <iostream>

int main() {
    int x = 5;
    std::cout << "Value of x: " << x << std::endl;

    if (x > 0) {
        std::cout << "x is greater than zero." << std::endl;
    } else {
        std::cout << "x is not greater than zero." << std::endl;
    }

    return 0;
}
